# Routes

## POST

`/api/signup`
`/api/page`
`/api/post`
`/api/login`: login do usuário

## GET

`/api/health`: checagem se a API está funcionando
`/api/validate`: validação do usuário
`/api/pages` -> ordernar por recentes (mais próximo da /home)
`/api/page/:pageId`: lê página por id
`/api/page/:pageId/posts`: lê os posts de uma página específica
`/api/page/:pageId/post/:postId`: lê um post específico de uma página

## Update



## Delete
